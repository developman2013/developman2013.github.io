{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Angular: debug in Chrome",
      "type": "pwa-chrome",
      "request": "launch",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "npm: start"
    }
  ]
}
